# 🚀 项目安装指南（Windows 超简单版）

本教程适合 **零基础用户**。  
按照步骤操作即可运行项目。

---

# 🧩 第一步：安装 Python

### 1. 检查是否已安装

按 `Win + R` → 输入 `cmd` → 回车  
然后输入：

```bash
python --version

如果显示版本号（例如 Python 3.11.6），跳至第三步。

### 2. 如果尚未安装python
前往https://www.python.org/downloads/windows/下载，并勾选`Add Python to PATH`；安装完成后检查python版本号

### 3. 创建python虚拟环境
命令行输入
```bash
python -m venv venv
创建成功后命令行中应出现(venv) 前缀

### 4. 安装项目依赖
在项目目录下运行
```bash
pip install -r requirements.txt

### 5. 运行程序
```bash
streamlit run app.py